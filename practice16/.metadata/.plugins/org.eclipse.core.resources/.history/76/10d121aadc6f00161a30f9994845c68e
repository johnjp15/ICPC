import java.util.ArrayList;
import java.util.Arrays;
import java.util.Scanner;

public class ProblemB {

	public static void main(String[] args)
	{
		ArrayList<Integer> arlist = new ArrayList<Integer>();
		Scanner sc = new Scanner(System.in);
		int numsets = sc.nextInt();
		for (int i = 0; i<numsets; i++) {
			sc.nextInt(); //Discard dataset number
			int numints = sc.nextInt();
			int max = 0;
			for (int j = 0; i<numints; j++) {
				int cur = sc.nextInt();
				arlist.add(cur);
				if (cur > max) max = cur;
			}
			//Time for processing
			int bucket = max;
			int k = 0;
			for (k = bucket; ; k++) {
				boolean solution = true;
				int tempbucket = k;
				int intpos = 0;
				while (intpos < numints) {
					tempbucket -= arlist.get(intpos);
					if (tempbucket < 0) {
						solution = false;
						break;
					}
					if (tempbucket == 0) tempbucket = k;
				}
				if (tempbucket != 0) solution = false;
				if (solution) break;
			}
			System.out.println(i + " " + k);
		}
	}

}
